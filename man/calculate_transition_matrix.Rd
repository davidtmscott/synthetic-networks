% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_transition_matrix.R
\name{calculate_transition_matrix}
\alias{calculate_transition_matrix}
\title{Calculate Transition Matrix from Patch Landscape}
\usage{
calculate_transition_matrix(landscape, dispersal, R)
}
\arguments{
\item{landscape}{A data frame or matrix with two columns (\code{x}, \code{y}) representing patch coordinates.}

\item{dispersal}{Numeric. Mean dispersal distance; controls the steepness of the dispersal kernel.}

\item{R}{Numeric. A global rate parameter that scales all transition values.}
}
\value{
A square numeric matrix where element \code{[i, j]} represents the transition rate from patch \code{i} to \code{j}.
}
\description{
Computes a matrix of transition rates between habitat patches based on their pairwise distances.
The rates are calculated using a negative exponential dispersal kernel, scaled by a global rate constant.
}
\details{
The transition rate between patches \code{i} and \code{j} is computed as:
\deqn{R \cdot \lambda^2 \cdot \exp(-\lambda \cdot d_{ij})}
where \code{d_{ij}} is the Euclidean distance between patches and \code{λ = 2 / dispersal}.
}
\examples{
landscape <- data.frame(x = runif(10, 0, 10), y = runif(10, 0, 10))
calculate_transition_matrix(landscape, dispersal = 2, R = 0.5)

}
